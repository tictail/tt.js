<div id="property_{{name}}" class="tt-method">
  <div class="desc-block">
    <h3>{{name}}</h3>
  </div>

  {{#if deprecated}}
    <p class="deprecated" title="{{deprecationMessage}}">{{deprecationMessage}}</p>
  {{/if}}

  <div class="description">
    {{{propertyDescription}}}
  </div>
</div>
